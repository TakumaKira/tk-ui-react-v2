---
description: 'A useThemeSwitcher React Hook.'
labels: ['hook', 'react']
---

A react hook for incrementing the state of count by 1 each time increment() is invoked.

## Get started

Install and use the hook in your components:

```tsx
import { Colors, colorsTokensDark } from '@takumakira-individual/tk-ui-react-v1.themes.colors';
import { useThemeSwitcher, Theme } from '@takumakira-individual/tk-ui-react-v1.hooks.use-theme-switcher';

export function MyComponent() {
  const theme = useThemeSwitcher();
  return (
    <Colors overrides={theme === Theme.DARK ? colorsTokensDark : undefined}>
      The count is {response.count}
    </Colors>
  );
}
```
