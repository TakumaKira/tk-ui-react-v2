---
labels: ['tk-ui-react-v2-service-express', 'express', 'server', 'microservice']
description: 'express server'
---

A tk-ui-react-v2-service-express component. Provides a RESTful API for the platform.

## Run the server 

Import the platform and use the it in your workspace:

```bash
bit use tk-ui-react-v2-service-express
```

Run the server:
```bash
bit run tk-ui-react-v2-service-express
```

## Compose to your platform

You can compose the server to a Platform app. Use the platform application type in a Bit app `my-platform.bit-app.ts` and include your server:
```ts
import { Platform } from '@bitdev/platforms.platform';

export const MyPlatform = Platform.from({
  name: 'my-platform',

  backends: {
    main: new AcmeGateway(),
    services: [
      TkUiReactV2ServiceExpress,
    ]
  },
});
```
